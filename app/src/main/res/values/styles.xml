<resources>

    <style name="MyMaterialTheme" parent="MyMaterialTheme.Base" />


    <!-- Base application theme. -->
    <style name="MyMaterialTheme.Base" parent="Theme.AppCompat.Light">

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:textColorPrimary">@color/white</item>

        <item name="android:actionBarWidgetTheme">@style/MyActionBarWidgetTheme</item>
        <item name="alertDialogTheme">@style/AppCompatAlertDialogStyle</item>

        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="MyActionBarWidgetTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="MyMaterialTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppCompatAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@color/teal_500</item>
    </style>

    <style name="ButtonRetourMenu" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/purple</item>
        <item name="colorButtonNormal">@color/orange</item>
    </style>

    <!-- Style pour le spinner permettant de choisir le food truck auquel voir les emplacements.-->
    <style name="Widget.MyApp.HeaderBar.Spinner" parent="Widget.AppCompat.Light.Spinner.DropDown.ActionBar">
        <item name="android:background">?android:selectableItemBackground</item>
        <item name="android:dropDownSelector">?android:selectableItemBackground</item>
        <item name="android:divider">@null</item>
    </style>

</resources>
